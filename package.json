{
  "name": "lowbar",
  "private": true,
  "version": "0.0.0",
  "dependencies": {},
  "scripts": {
    "test": "mocha ./spec/",
    "lint": "eslint ./",
    "lint:fix": "npm run lint -- --fix",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.15.0"
  }
}
