{
  "name": "lowbar",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "mocha spec/*.spec.js",
    "lint": "eslint ./",
    "lint:fix": "npm run lint -- --fix",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.15.0",
    "mocha": "^4.0.1"
  }
}
